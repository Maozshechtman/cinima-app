import {React,useEffect,useState} from 'react';

const Users = () => {

    //State configs
    const [users,setUsers] = useState([]);

    let items = users.map();
    return (
        <div>
            //Todo: create a grid and create new commpont for single user
        </div>
    );
};

export default Users;